{
  "name": "@foresight-cdss-next/functions-api",
  "version": "1.0.0",
  "description": "API Lambda functions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:functions": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "check-types": "tsc --noEmit",
    "test": "echo 'No tests specified'"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@aws-sdk/credential-providers": "^3.911.0",
    "@foresight-cdss-next/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "*",
    "typescript": "*"
  }
}
