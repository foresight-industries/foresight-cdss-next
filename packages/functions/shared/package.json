{
  "name": "@foresight-cdss-next/functions-lambda-shared",
  "version": "1.0.0",
  "description": "Shared lambda data processing utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "build:functions": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "check-types": "tsc --noEmit",
    "test": "echo 'No tests specified'"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "*",
    "typescript": "*"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.914.0"
  }
}
